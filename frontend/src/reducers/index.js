import { combineReducers } from 'redux'

const postReducer = (state = {}, action) => {
    switch(action.type){
        
        default:
            return state;
    }
}


export default combineReducers({
    postReducer,
})

